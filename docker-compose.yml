services:
  valkey:
    image: valkey/valkey-bundle:latest
    ports:
      - "6399:6379"
    command: ["valkey-server", "--loadmodule", "/opt/valkey-stack/lib/libsearch.so"]
    volumes:
      - valkey-data:/data

volumes:
  valkey-data:
