{"q":"What is Valkey?","a":"Valkey is an open-source, high-performance key-value data store forked from Redis. It is maintained by the Linux Foundation and provides full Redis OSS compatibility while remaining truly open-source under the BSD 3-Clause license. Valkey supports data structures like strings, hashes, lists, sets, sorted sets, streams, and more."}
{"q":"How does Valkey differ from Redis?","a":"Valkey is a community fork of Redis OSS created in 2024 after Redis changed its license. Key differences: Valkey is BSD-licensed (truly open-source), developed by the Linux Foundation community, and receives contributions from major cloud providers. Redis is now dual-licensed (SSPL + RSALv2). Functionally, Valkey 7.2 and Redis 7.2 OSS are nearly identical, but Valkey 8.x introduces new features like IO threads."}
{"q":"How do I install valkey-search?","a":"valkey-search is the Valkey module for full-text and vector search. Install it by: 1) Using the valkey-bundle Docker image which includes all modules pre-loaded, or 2) Building from source at github.com/valkey-io/valkey-search and loading it with --loadmodule /path/to/libsearch.so. The module adds FT.CREATE, FT.SEARCH, FT.AGGREGATE, and vector similarity search commands."}
{"q":"What is the XADD command?","a":"XADD appends a new entry to a stream. Syntax: XADD key [NOMKSTREAM] [MAXLEN|MINID [=|~] threshold [LIMIT count]] *|id field value [field value ...]. The * auto-generates an ID. Returns the generated stream ID. Example: XADD mystream * name Alice age 30"}
{"q":"How does FT.SEARCH work?","a":"FT.SEARCH performs full-text and vector searches on a RediSearch/valkey-search index. Syntax: FT.SEARCH index query [RETURN fields] [SORTBY field] [LIMIT offset count]. For vector KNN search use: FT.SEARCH idx '(filter)=>[KNN k @vec $BLOB]' PARAMS 2 BLOB <embedding bytes> DIALECT 2."}
{"q":"What persistence options does Valkey support?","a":"Valkey supports three persistence modes: 1) RDB snapshots - point-in-time snapshots saved to disk at configured intervals (save 900 1 saves if 1 key changed in 15 min). 2) AOF (Append Only File) - logs every write operation, reconstructable on restart. 3) RDB+AOF combined mode for best durability. Configure with save, appendonly, and appendfsync directives in valkey.conf."}
{"q":"How do sorted sets work in Valkey?","a":"Sorted sets (ZSET) store unique members each associated with a floating-point score. Members are ordered by score. Key commands: ZADD key score member, ZRANGE key start stop [WITHSCORES], ZRANGEBYSCORE key min max, ZRANK key member (0-indexed rank), ZREM key member, ZINCRBY key increment member. Sorted sets are O(log N) for most operations and useful for leaderboards, priority queues, and range queries."}
{"q":"What is the difference between HSET and HMSET?","a":"HSET sets one or more fields in a hash: HSET key field1 value1 [field2 value2...]. As of Redis 4.0 / Valkey, HSET accepts multiple field-value pairs. HMSET is deprecated since Redis 4.0 — HSET is the replacement. Use HSET for all new code. HGETALL retrieves all fields and values, HGET retrieves a single field."}
{"q":"How does Valkey replication work?","a":"Valkey uses primary-replica (formerly master-slave) replication. Configure with REPLICAOF host port or replica-of in valkey.conf. Full resync on first connection: primary sends RDB snapshot, replica loads it, then streams commands. Partial resync (PSYNC) reconnects without full dump if the replica's replication offset is within the replication backlog. Check replication status with INFO replication."}
{"q":"What is a Bloom filter in Valkey?","a":"A Bloom filter (valkey-bloom module) is a probabilistic data structure for membership testing with no false negatives but possible false positives. Commands: BF.ADD key item, BF.EXISTS key item, BF.MADD for bulk adds, BF.MEXISTS for bulk checks. Create with BF.RESERVE key error_rate capacity. Useful for checking if a user has seen content or if an email is in a blocklist without storing the full set."}
{"q":"How do I configure Valkey clustering?","a":"Valkey Cluster distributes data across multiple nodes automatically. Enable with cluster-enabled yes in valkey.conf. Use redis-cli --cluster create or valkey-cli --cluster create to initialize. Cluster uses 16384 hash slots. Each key maps to a slot: slot = CRC16(key) % 16384. Use {} hash tags to force keys to the same slot. Minimum 3 primary nodes recommended for production."}
{"q":"What is the TTL command?","a":"TTL returns the remaining time-to-live of a key in seconds. Returns -1 if the key has no TTL (persists forever), -2 if the key does not exist. PTTL returns TTL in milliseconds. Set TTL with EXPIRE key seconds, PEXPIRE key milliseconds, EXPIREAT key unix-timestamp, or inline with SET key value EX seconds PX milliseconds."}
{"q":"How does MULTI/EXEC work in Valkey?","a":"MULTI/EXEC provides transactions in Valkey. MULTI begins a transaction block. Commands queued after MULTI are not executed immediately — they return QUEUED. EXEC executes all queued commands atomically. DISCARD aborts. WATCH key monitors keys for changes; if a watched key changes before EXEC, the transaction aborts (returns nil). Note: errors in queued commands don't abort the transaction automatically."}
{"q":"What is LRU eviction in Valkey?","a":"Valkey evicts keys when maxmemory is reached. Eviction policies: noeviction (return error), allkeys-lru (LRU across all keys), volatile-lru (LRU among keys with TTL), allkeys-random, volatile-random, volatile-ttl (evict soonest-expiring), allkeys-lfu, volatile-lfu. Set with maxmemory 256mb and maxmemory-policy allkeys-lru in config. LFU (Least Frequently Used) is often better than LRU for skewed workloads."}
{"q":"How do I use Valkey Pub/Sub?","a":"Pub/Sub in Valkey uses SUBSCRIBE, PUBLISH, and UNSUBSCRIBE. SUBSCRIBE channel [channel...] subscribes a client to channels. PUBLISH channel message delivers to all subscribers. PSUBSCRIBE pattern uses glob patterns (news.* matches news.sports, news.tech). PUBSUB CHANNELS lists active channels. PUBSUB NUMSUB channel shows subscriber counts. Note: messages are fire-and-forget — use Streams for persistent message queues."}
{"q":"What is @betterdb/agent-cache?","a":"@betterdb/agent-cache is a multi-tier exact-match cache for AI agent workloads backed by Valkey. It caches LLM responses, tool results, and session state in one package with built-in OpenTelemetry tracing and Prometheus metrics. Works on vanilla Valkey 7+ with no module requirements. Integrates with LangChain, LangGraph, and Vercel AI SDK."}
{"q":"What is @betterdb/semantic-cache?","a":"@betterdb/semantic-cache is a Valkey-native semantic cache for LLM applications. It uses valkey-search vector similarity to match semantically similar prompts and return cached responses. Uses cosine distance (not similarity) for thresholding. Built-in OpenTelemetry and Prometheus instrumentation. Requires Valkey 8+ with valkey-search module."}
{"q":"How do I use SCAN instead of KEYS?","a":"SCAN iterates keys incrementally without blocking, unlike KEYS which scans the whole keyspace atomically. Usage: SCAN cursor [MATCH pattern] [COUNT count] [TYPE type]. Start with cursor 0; use the returned cursor in the next call until cursor returns 0 (full iteration). COUNT is a hint, not a guarantee. HSCAN, SSCAN, ZSCAN iterate hash fields, set members, and sorted set members respectively."}
{"q":"What data structures does Valkey support?","a":"Valkey native data structures: Strings, Lists (doubly-linked, used with LPUSH/RPUSH/LRANGE), Hashes (field-value maps), Sets (unique members, SADD/SMEMBERS/SINTER), Sorted Sets (scored members, ZADD/ZRANGE), Streams (append-only log, XADD/XREAD), Bitmaps (bit-level string operations), HyperLogLog (probabilistic cardinality). With modules: JSON (valkey-json), Bloom/Cuckoo filters (valkey-bloom), full-text + vector search (valkey-search)."}
{"q":"How does pipeline work in Valkey?","a":"Pipelining sends multiple commands to Valkey in one network round-trip without waiting for individual replies. In iovalkey: const p = client.pipeline(); p.set('a', 1); p.get('a'); const results = await p.exec(). Results are an array of [error, value] tuples. Pipelining is not transactional (unlike MULTI/EXEC) but dramatically reduces RTT overhead for bulk operations. Batch sizes of 100-1000 are typical."}
